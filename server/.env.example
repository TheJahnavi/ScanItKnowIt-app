# Environment variables for ScanItKnowIt application

# Required for JWT token generation
JWT_SECRET=your_generated_jwt_secret_here

# Required for AI functionality (OpenRouter API)
OPENROUTER_API_KEY=sk-or-v1-your_openrouter_api_key_here

# Optional: Reddit API credentials for product reviews
REDDIT_CLIENT_ID=your_reddit_client_id_here
REDDIT_CLIENT_SECRET=your_reddit_client_secret_here
REDDIT_USER_AGENT=ScanItKnowIt/1.0

# Optional: Custom port (default is 3002 for local development)
PORT=3002

# Optional: CORS origin for development
CORS_ORIGIN=http://localhost:3001