{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "builds": [
    {
      "src": "server/vercel-entry.ts",
      "use": "@vercel/node",
      "config": {
        "tsconfig": "server/tsconfig.json"
      }
    },
    {
      "src": "client",
      "use": "@vercel/static",
      "output": "dist"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server"
    },
    {
      "src": "/(.*)",
      "dest": "client",
      "methods": ["GET"]
    }
  ]
}